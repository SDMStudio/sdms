
/*=============================================================================
Copyright (C) 2016 Jilles Steeve Dibangoye
==============================================================================*/

#include <sdm/types.hpp>
#include <sdm/utils/decision_rules/joint.hpp>

namespace sdm
{

    // template <typename item>
    // Joint<item>::Joint(const std::vector<item> &joint_item) : num_agents_(joint_item.size()), std::vector<item>(joint_item)
    // {
    // }

    // template <typename item>
    // Joint<item>::Joint(const std::vector<number> &num_agents, const std::vector<item> &joint_item) : num_agents_(num_agents.size()), std::vector<item>(joint_item) {}

    // template class Joint<number>;

    // typedef Joint<number> JointItem;
} // namespace sdm
